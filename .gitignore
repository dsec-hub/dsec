# ---- Python (dsec-api) ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- Node / Next.js (dsec-website, dsec-app) ----
node_modules/
.next/
out/
build/
.turbo/
*.tsbuildinfo

# ---- Env / secrets (NEVER commit real secrets) ----
# Note: .env.example is intentionally NOT ignored — it is committed.
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
.env.test.local

# ---- Local databases ----
*.db
*.sqlite
*.sqlite3

# ---- Vercel ----
.vercel/

# ---- Sticker assets (locally generated AI art, not part of the app) ----
sticker-assets/

# ---- OS / editors ----
.DS_Store
.idea/
.vscode/
*.swp
*~

# ---- Local AI-assistant / agent tooling (not part of the project) ----
# Per-tool skill caches downloaded locally; should not be published.
.claude/
.cursor/
.gemini/
.kiro/
.opencode/
.qoder/
.agents/
skills-lock.json

# generated sponsor logo assets (drag into dashboard, not committed)
sponsor-logos/

# throwaway flagship concept prototypes (reference only, not shipped)
flagship-demos/
